apiVersion: v1
kind: Service
metadata:
  name: {{ include "portfolio.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "portfolio.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
